# bunienjoku.github.io

Personal website — [bunienjoku.github.io](https://bunienjoku.github.io).

Static HTML, CSS and vanilla JavaScript; no build step. The animated
backgrounds are a small WebGL fragment shader (`js/fluid.js`).

To preview locally:

```
python3 -m http.server 8000
```
